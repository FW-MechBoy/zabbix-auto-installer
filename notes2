Después este:

systemctl list-units --type=service --all | egrep -i 'postgres|pgsql|patroni|edb'

Y este:

ps -ef | egrep -i 'postgres|postmaster|patroni' | grep -v grep

Por último, revisemos si hay algo escuchando en puertos típicos:

ss -lntp | egrep '5432|5433|5434|postgres|postmaster'
