find / -type f \( -iname '*.rpm' -o -iname '*timescale*.tar*' -o -iname '*timescale*.zip' -o -iname '*gcc*.rpm' -o -iname '*cmake*.rpm' -o -iname '*make*.rpm' \) 2>/dev/null | sort

find /var/cache/dnf /var/cache/yum /msr /tmp /opt -type f -iname '*.rpm' 2>/dev/null | sort
