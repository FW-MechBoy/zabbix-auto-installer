mccli client show \
  --domain=/REPLICATE \
  --recursive=true \
  --replicated=true \
  --verbose=true


grep -RhiE 'dstaddr|destination.*(host|server)|target.*(host|server)' \
  /usr/local/avamar/var/client/*Replicate*.log 2>/dev/null | tail -30

ls -lt /usr/local/avamar/var/client/*Replicate* 2>/dev/null | head -20


mccli client show --domain=/MC_SYSTEM --recursive=true --verbose=true


[BaaS] Failed DIMM B7 replacement – ohlpch2bavas001

Perform the physical replacement of the failed Dual Inline Memory Module (DIMM) installed in memory slot B7 on Avamar system ohlpch2bavas001.

System information:
- Hostname: ohlpch2bavas001
- Serial number: 935QY64
- Model: Avamar Data Store Gen5A
- Affected component: DIMM B7
- Scheduled date: Monday, July 27, 2026
- Customer contact: Gervaix Christopher – gervaix@pbs-santander.com

Reported hardware errors:

UEFI0339: The Dual Inline Memory Module (DIMM) in memory slot B7 is disabled because of initialization errors caused by uncorrectable memory errors, an invalid configuration, or other hardware conditions.

UEFI0098: The memory Built-in Self-test (BIST) detected one or more errors on the DIMM installed in memory slot B7. The affected DIMM has been disabled as a result.

The activity requires an approved maintenance window. Backup and restore activities must be stopped before execution, as the Avamar system will be shut down and temporarily unavailable during the hardware replacement.

No active replication configuration or replicated clients were identified on this Avamar system during the CLI validation.

A physical component replacement by an authorized Dell Field Service Engineer is required.



Corrective hardware replacement is required for Avamar system ohlpch2bavas001.

The DIMM installed in memory slot B7 has reported uncorrectable initialization and Built-in Self-test errors. The system firmware has disabled the affected memory module to prevent further hardware instability.

The DIMM must be replaced according to the Dell documented procedure to restore the supported memory configuration, hardware redundancy, system stability, and full memory capacity.
